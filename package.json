{
  "name": "required-approvals",
  "version": "5.0.0",
  "private": true,
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "test": "node --test"
  },
  "dependencies": {
    "@octokit/rest": "^22.0.0",
    "minimatch": "^10.0.0"
  }
}
